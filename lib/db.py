# Functions for interacting with the database

# Confirm we're in the right file
def database():
	print('Date a Bass?')

# params
# fields: list of ticket fields to retriev
# filters: dictionary of field:value:comparison
# order: string, field to order by
# limit: int, how many items to retrieve
# ascending: bool, which way to erder
def get_ticket_data(fields, filters, limit, order, ascending = 1):
	print("table={} \nfields={} \nlimit={}".format(table, fields, limit))

	data=[ \
	{, \
	2, \
	3 \
	]

	return(data)
	
